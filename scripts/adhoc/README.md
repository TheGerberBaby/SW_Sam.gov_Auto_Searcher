# Ad-hoc Scripts

These scripts are historical one-off filters kept for reference. They are not
part of the default technical-services lead workflow and may contain fixed
dates or experimental scoring rules.

Use the maintained entry points in `scripts/scoring.py`, `scripts/search_bulk.py`,
and `scripts/swcb.py` for current research.
